<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Rede — necessário para API, streaming HLS e o proxy local de decriptação -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Manter ecrã ligado durante reprodução -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="Pixgo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <!--
          Desativa o Impeller (motor gráfico novo do Flutter no Android).
          Bug conhecido: em várias GPUs Android (Mali e outras comuns em
          aparelhos de gama média), o Impeller desenha blocos cinzentos
          sólidos sobre TextFields e outros widgets em vez do conteúdo real
          — exatamente o sintoma reportado. O Skia (motor antigo) não tem
          este problema e é o que a app vai usar a partir de agora.
        -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <!-- Necessário para url_launcher abrir links externos (ex: suporte/legal) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
        </intent>
    </queries>
</manifest>
